"""FastAPI web backend for the BTC swing cycle tracker.

Serves the React frontend, exposes the analysis pipeline as a JSON API, and
hosts the conversational agent pane (ported from the opportunity_pipeline
project's AgentPanel / agent_chat stack).
"""
